// Stub for prim_assert.sv - assertions not needed for parsing/simulation
`define ASSERT(NAME, PROP)
`define ASSERT_IF(NAME, PROP, EN)
`define ASSERT_INIT(NAME, PROP)
`define ASSERT_KNOWN(NAME, SIG)
`define ASSERT_KNOWN_IF(NAME, SIG, EN)
